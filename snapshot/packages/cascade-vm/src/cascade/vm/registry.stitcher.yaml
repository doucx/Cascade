CodeRegistry: 'The runtime registry for executable code.

  It maps canonical code structure hashes to actual Python callables.'
get: 'Retrieves a function by its canonical hash.

  Raises KeyError if not found.'
register: 'Registers a function under its canonical hash.

  If the hash already exists, we assume the code is identical (idempotent).'
