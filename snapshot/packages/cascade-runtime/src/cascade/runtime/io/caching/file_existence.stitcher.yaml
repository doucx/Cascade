FileExistenceCache: A simple cache policy that considers a task 'cached' if a specific
  file exists.
__init__: "Args:\n    target_path: A string path or a function that accepts task inputs\n\
  \                 (args/kwargs dict) and returns a path string."
